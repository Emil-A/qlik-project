<h1>Palindromes:</h1>
<%= form_for(@palindrome) do |f| %>
  <%= f.label :user, "Post user" %>
  <%= f.text_field :user%>

  <%= f.label :body, "Palindrome" %>
  <%= f.text_field :body%>

  <%= f.submit "Add"%>
<% end %>